# Copy this file to ".env" in the project root and fill in real keys (".env" is gitignored).
# Spring maps these to riot.api.key / gemini.api.key via application.properties placeholders.

RIOT_API_KEY=
GEMINI_API_KEY=
